"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.normalizeOptions = void 0;
const assertion_1 = require("../../../utils/assertion");
const devkit_1 = require("@nrwl/devkit");
function normalizeOptions(host, options) {
    var _a, _b, _c, _d;
    const appDirectory = options.directory
        ? `${devkit_1.names(options.directory).fileName}/${devkit_1.names(options.name).fileName}`
        : devkit_1.names(options.name).fileName;
    const appProjectName = appDirectory.replace(new RegExp('/', 'g'), '-');
    const e2eProjectName = `${appProjectName}-e2e`;
    const { appsDir } = devkit_1.getWorkspaceLayout(host);
    const appProjectRoot = devkit_1.normalizePath(`${appsDir}/${appDirectory}`);
    const parsedTags = options.tags
        ? options.tags.split(',').map((s) => s.trim())
        : [];
    const fileName = options.pascalCaseFiles ? 'App' : 'app';
    const styledModule = /^(css|scss|less|styl|none)$/.test(options.style)
        ? null
        : options.style;
    assertion_1.assertValidStyle(options.style);
    options.routing = (_a = options.routing) !== null && _a !== void 0 ? _a : false;
    options.classComponent = (_b = options.classComponent) !== null && _b !== void 0 ? _b : false;
    options.unitTestRunner = (_c = options.unitTestRunner) !== null && _c !== void 0 ? _c : 'jest';
    options.e2eTestRunner = (_d = options.e2eTestRunner) !== null && _d !== void 0 ? _d : 'cypress';
    return Object.assign(Object.assign({}, options), { name: devkit_1.names(options.name).fileName, projectName: appProjectName, appProjectRoot,
        e2eProjectName,
        parsedTags,
        fileName,
        styledModule, hasStyles: options.style !== 'none' });
}
exports.normalizeOptions = normalizeOptions;
//# sourceMappingURL=normalize-options.js.map