import { ExecutorContext } from '@nrwl/devkit';
export interface FileServerOptions {
    host: string;
    port: number;
    ssl: boolean;
    sslKey?: string;
    sslCert?: string;
    proxyUrl?: string;
    buildTarget: string;
    parallel: boolean;
    maxParallel: number;
    withDeps: boolean;
}
export default function (opts: FileServerOptions, context: ExecutorContext): Promise<{
    success: boolean;
}>;
