import { ExecutorContext } from '@nrwl/devkit';
import * as rollup from 'rollup';
import { DependentBuildableProjectNode } from '@nrwl/workspace/src/utilities/buildable-libs-utils';
import { PackageBuilderOptions } from '../../utils/types';
import { NormalizedBundleBuilderOptions } from '../../utils/normalize';
export default function run(rawOptions: PackageBuilderOptions, context: ExecutorContext): AsyncIterableIterator<{
    success: boolean;
}> | Promise<{
    success: boolean;
} | {
    success: boolean;
}>;
export declare function createRollupOptions(options: NormalizedBundleBuilderOptions, dependencies: DependentBuildableProjectNode[], context: ExecutorContext, packageJson: any, sourceRoot: string): rollup.InputOptions[];
