import { ExecutorContext } from '@nrwl/devkit';
export interface WebDevServerOptions {
    host: string;
    port: number;
    publicHost?: string;
    ssl: boolean;
    sslKey?: string;
    sslCert?: string;
    proxyConfig?: string;
    buildTarget: string;
    open: boolean;
    liveReload: boolean;
    watch: boolean;
    allowedHosts: string;
    maxWorkers?: number;
    memoryLimit?: number;
    baseHref?: string;
}
export default function devServerExecutor(serveOptions: WebDevServerOptions, context: ExecutorContext): AsyncIterableIterator<{
    stats: import("webpack").Stats;
    baseUrl: string;
    success: boolean;
}>;
