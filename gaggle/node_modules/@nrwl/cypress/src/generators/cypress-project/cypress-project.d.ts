import { Tree } from '@nrwl/devkit';
import { Schema } from './schema';
export interface CypressProjectSchema extends Schema {
    projectName: string;
    projectRoot: string;
}
export declare function cypressProjectGenerator(host: Tree, schema: Schema): Promise<any>;
export default cypressProjectGenerator;
export declare const cypressProjectSchematic: (options: Schema) => (tree: any, context: any) => Promise<any>;
