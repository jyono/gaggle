import * as webpack from 'webpack';
import { Stats, Configuration } from 'webpack';
import { Observable } from 'rxjs';
export declare function runWebpack(config: Configuration): Observable<Stats>;
export declare function runWebpackDevServer(config: Configuration): Observable<{
    stats: Stats;
    baseUrl: string;
}>;
export interface EmittedFile {
    id?: string;
    name?: string;
    file: string;
    extension: string;
    initial: boolean;
    asset?: boolean;
}
export declare function getEmittedFiles(stats: webpack.Stats): EmittedFile[];
