"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.updateTsconfig = void 0;
const devkit_1 = require("@nrwl/devkit");
/**
 * Updates the tsconfig paths to remove the project.
 *
 * @param schema The options provided to the schematic
 */
function updateTsconfig(tree, schema, project) {
    const { npmScope } = devkit_1.getWorkspaceLayout(tree);
    const tsConfigPath = 'tsconfig.base.json';
    if (tree.exists(tsConfigPath)) {
        devkit_1.updateJson(tree, tsConfigPath, (json) => {
            delete json.compilerOptions.paths[`@${npmScope}/${project.root.substr(5)}`];
            return json;
        });
    }
}
exports.updateTsconfig = updateTsconfig;
//# sourceMappingURL=update-tsconfig.js.map