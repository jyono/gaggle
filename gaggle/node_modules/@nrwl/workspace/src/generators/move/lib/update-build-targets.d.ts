import { Tree } from '@nrwl/devkit';
import { Schema } from '../schema';
/**
 * Update other references to the source project's targets
 */
export declare function updateBuildTargets(tree: Tree, schema: Schema): void;
